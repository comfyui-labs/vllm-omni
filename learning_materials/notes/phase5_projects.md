# 阶段五：实践项目（持续）

目标：在真实任务中巩固理解并开始贡献代码。

## 小项目清单

1. 改写一个示例，增加可配置的采样参数
2. 给 `examples/` 增加一个新的可运行脚本（对照现有模型）
3. 阅读并运行 `tests/e2e` 关键测试
4. 选一个 Diffusion 模型，写一页“模型结构 + 推理流程”笔记

## 贡献前准备

- 阅读 `docs/contributing/model/adding_diffusion_model.md`
- 跑通与目标模块相关的测试
- 输出一个小的性能/显存对比报告
